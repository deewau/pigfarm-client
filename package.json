{
  "name": "pigfarm",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "tunnel": "lt --port 5173",
    "start": "cd server && node dist/index.js",
    "build": "npm install && npm run client:build && npm run server:build",
    "client:build": "cd client && npm install && npx vite build",
    "server:build": "cd server && npm install && npx shx rm -rf public && npx shx cp -r ../client/dist public/ && tsc"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "localtunnel": "^2.0.2"
  }
}
}
